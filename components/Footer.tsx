import Heading from "./Heading";
import { FC } from 'react';

const Footer: FC = () => {
  return (
    <footer>
      <Heading tag={'h3'} text={'Footer'} />
    </footer>
  );
}

export default Footer;
